# Replace {{ .Values.DB_ROLE  }} with the name of the database role SAS Viya will use.
username={{ .Values.DB_ROLE  }}
# Replace {{ .Values.DB_ROLE_PASSWORD }} with the password of the database role SAS Viya will use.
password={{ .Values.DB_ROLE_PASSWORD }}
