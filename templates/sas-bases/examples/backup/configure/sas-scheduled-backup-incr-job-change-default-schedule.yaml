#   # NOTE:
#   # {{ .VALUES.SCHEDULE_BACKUP_CRON_EXPRESSION }} should be the cron expression to replace default schedule of incremental backup cronjob.
apiVersion: builtin
kind: PatchTransformer
metadata:
  name: sas-scheduled-backup-incr-job-change-default-schedule
patch: |-
  - op: replace
    path: /spec/schedule
    value: "{{ .VALUES.SCHEDULE_BACKUP_CRON_EXPRESSION }}"
target:
  kind: CronJob
  name: sas-scheduled-backup-incr-job
  version: v1
