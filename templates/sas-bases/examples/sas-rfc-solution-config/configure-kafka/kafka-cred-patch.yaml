apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-cred-patch
data:
  patch.yaml: |
    - op: replace
      path: /data
      value:
        username: {{ .Values.BASE64_ENCODED_USER_NAME }}
        password: {{ .Values.BASE64_ENCODED_PASSWORD }}
