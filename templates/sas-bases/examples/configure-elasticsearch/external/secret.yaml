---
apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
  name: sas-opendistro-sasadmin-secret
stringData:
  # Replace {{.Values.USERNAME}}
  username: "{{ .Values.USERNAME }}"
  with the username of an account with administrative privileges for your external OpenSearch instance username: "{{ .Values.USERNAME }}"

"
  # Replace {{.Values.PASSWORD}}
 with the password of an account with administrative privileges for your external OpenSearch instance
  password: "{{.Values.PASSWORD}}
"
