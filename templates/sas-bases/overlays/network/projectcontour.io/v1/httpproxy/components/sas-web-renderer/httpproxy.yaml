---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  annotations:
    sas.com/kustomize-base: base
  labels:
    app.kubernetes.io/name: sas-web-renderer
    sas.com/admin: namespace
  name: sas-web-renderer
spec:
  routes:
  - conditions:
    - prefix: /webRenderer
    loadBalancerPolicy:
      strategy: Cookie
    pathRewritePolicy:
      replacePrefix:
      - replacement: /webRenderer/
    services:
    - name: sas-web-renderer
      port: 80
    timeoutPolicy:
      response: 300s
  - conditions:
    - prefix: /webRenderer/
    loadBalancerPolicy:
      strategy: Cookie
    services:
    - name: sas-web-renderer
      port: 80
    timeoutPolicy:
      response: 300s

