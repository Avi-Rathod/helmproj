STORAGE_ACCOUNT_NAME: "SOME NAME SAAS"
NFS_MOUNT_PATH: "ABC"
NFS_SERVER_LOCATION: "Location"
NFS_SERVER: "SERVER"
NFS_SERVER_ADDR: "add"
NFS_SERVER_PATH: "path"
EXISTING_K8S_PVC_NAME: "EXISTING"
PVC_NAME :"pvc"

replicaCount: 2

image:
  repository: nginx
  tag: "1.16.0"
  pullPolicy: IfNotPresent

service:
  name: nginx-service
  type: ClusterIP
  port: 80
  targetPort: 9000

env:
  name: dev