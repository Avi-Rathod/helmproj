---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: sas-detection-message-schema
spec:
  host: sas-detection-message-schema
  trafficPolicy:
    loadBalancer:
      consistentHash:
        httpCookie:
          name: sas-ingress-istio
          path: /detectionMessageSchema/
          ttl: 0s

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  annotations:
    sas.com/kustomize-base: base
  labels:
    app.kubernetes.io/name: sas-detection-message-schema
    sas.com/admin: namespace
  name: sas-detection-message-schema
spec:
  gateways:
  - public-gateway
  hosts:
  - '*'
  http:
  - match:
    - uri:
        exact: /detectionMessageSchema
    - uri:
        prefix: /detectionMessageSchema/
    route:
    - destination:
        host: sas-detection-message-schema
    timeout: 300s

