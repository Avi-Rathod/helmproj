---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  annotations:
    sas.com/kustomize-base: base
  labels:
    app.kubernetes.io/name: sas-detection-message-schema
    sas.com/admin: namespace
  name: sas-detection-message-schema
spec:
  routes:
  - conditions:
    - prefix: /detectionMessageSchema
    loadBalancerPolicy:
      strategy: Cookie
    pathRewritePolicy:
      replacePrefix:
      - replacement: /detectionMessageSchema/
    services:
    - name: sas-detection-message-schema
      port: 80
    timeoutPolicy:
      response: 300s
  - conditions:
    - prefix: /detectionMessageSchema/
    loadBalancerPolicy:
      strategy: Cookie
    services:
    - name: sas-detection-message-schema
      port: 80
    timeoutPolicy:
      response: 300s

