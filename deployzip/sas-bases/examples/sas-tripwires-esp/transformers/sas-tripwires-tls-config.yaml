apiVersion: builtin
kind: PatchTransformer
metadata:
  name: sas-tls-config
target:
  kind: ESPServer
patch: |-
  - op: add
    path: /spec/projectTemplate/deployment/spec/template/spec/containers/0/envFrom/-
    value:
      configMapRef:
        name: sas-tls-config